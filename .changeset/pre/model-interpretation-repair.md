---
'@ontahi/core': patch
---

Prefer declared operations over lower-level graph commands in model context and output contracts, and give interpreters one bounded repair attempt when a structurally valid proposal fails scoped runtime validation.
