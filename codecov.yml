coverage:
  status:
    project: off
    patch:
      default:
        target: auto
        threshold: 1%

comment:
  layout: reach,diff,files
  behavior: default
  require_changes: true

ignore:
  - apps/**
  - examples/**
  - fixtures/**
