# Ontahi Atlas

`items/` contains Ontahi's durable framework model. `sources.yaml` registers repositories used by
canonical cross-source references. See
[`docs/planning-and-atlas.md`](../docs/planning-and-atlas.md).
