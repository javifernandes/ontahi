---
---

Split long Console and Explorer integration tests into focused scenarios, including independent unsafe-query limit cases, preserving their assertions and default timeouts under CI coverage instrumentation.
