---
'@ontahi/core': minor
---

Author structural Relationship Commands directly from typed Entity Refs with cardinality-specific
relation methods such as `student.course.assign(course)` and `course.students.add(student)`.
