---
'@ontahi/core': minor
---

Expose a versioned JSON-safe data graph read request that preserves Selection, View, ordering,
limit, cardinality, and read mode and can be rebuilt against server-owned Entity definitions.
