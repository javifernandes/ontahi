---
'@ontahi/runtime-nextjs': minor
---

Add an App Router graph-read handler that validates canonical requests, derives authority from a
trusted invocation context, and preserves the Express adapter's protocol status semantics.
